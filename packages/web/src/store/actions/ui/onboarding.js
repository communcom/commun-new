import { CLOSE_ONBOARDING_BANNER } from 'store/constants';

// eslint-disable-next-line import/prefer-default-export
export const closeOnboardingBanner = () => ({
  type: CLOSE_ONBOARDING_BANNER,
});
